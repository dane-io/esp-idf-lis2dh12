# LIS2DH12 accelerometer driver for ESP IDF

## Install via git submodule
```bash
git submodule add https://github.com/dane-io/esp-idf-lis2dh12.git components/lis2dh12
git submodule update --init --recursive
```